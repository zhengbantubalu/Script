{
  "name": "scripts-web-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5173",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "vite": "^5.4.10"
  }
}

